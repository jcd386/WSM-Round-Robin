<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <deploymentStatus>Deployed</deploymentStatus>
    <description>One WHERE clause row on a Capacity Query, e.g. "Employer_Status__c equals Contracted". Filter Number is what the parent's Filter Logic refers to. Field names and operators are validated against the org schema and a closed operator list before any query runs, and every value is passed as a bind variable, so nothing an admin types is ever concatenated into SOQL.</description>
    <enableActivities>false</enableActivities>
    <enableBulkApi>true</enableBulkApi>
    <enableFeeds>false</enableFeeds>
    <enableHistory>false</enableHistory>
    <enableReports>true</enableReports>
    <enableSearch>true</enableSearch>
    <enableSharing>true</enableSharing>
    <enableStreamingApi>true</enableStreamingApi>
    <label>Capacity Filter</label>
    <nameField>
        <displayFormat>CAPF-{00000}</displayFormat>
        <label>Capacity Filter Number</label>
        <type>AutoNumber</type>
    </nameField>
    <pluralLabel>Capacity Filters</pluralLabel>
    <sharingModel>ControlledByParent</sharingModel>
    <visibility>Public</visibility>
</CustomObject>
